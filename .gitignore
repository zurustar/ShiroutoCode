# macOS
.DS_Store

# Serena (optional per-developer tooling)
.serena/

# Go build artifacts
/bin/
*.out
# Built binary at repo root only — anchored so it does NOT match the
# cmd/shiroutocode/ source directory (which must stay tracked).
/shiroutocode

# GoReleaser output
/dist/
